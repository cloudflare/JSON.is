default:
  files: '*'

root:
  bucket: 'json.is'
  root: 'build'

install:
  bucket: 'install.json.is'
  root: 'build/install'

package:
  bucket: 'package.json.is'
  root: 'build/package'

bower:
  bucket: 'bower.json.is'
  root: 'build/bower'
