default:
  files: 'images/*,index.html'

root:
  bucket: 'json.is'
  root: 'build'

install:
  bucket: 'install.json.is'
  root: 'build/install'

package:
  bucket: 'package.json.is'
  root: 'build/package'
